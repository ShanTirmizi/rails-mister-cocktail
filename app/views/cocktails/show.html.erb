<p><%= @cocktail.name %></p>

<% @cocktail.doses.each do |dose|  %>
 <ul>
   <li><%=  dose.description  %> <%= dose.ingredient.name %></li>
  </ul>
  <%= link_to "delete", dose_path(dose), method: :delete, data: { confirm: "Are you sure?" } %>
<% end %>



<%= link_to "Add dose", new_cocktail_dose_path(@cocktail, Dose.new) %> 
